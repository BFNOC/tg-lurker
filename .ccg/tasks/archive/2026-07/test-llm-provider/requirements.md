# LLM 上游连接测试

- 在每个 LLM 上游卡片内提供测试按钮。
- 测试使用该卡片当前填写、尚未保存的 Base URL、API Key、模型列表中的第一个模型和 API 格式。
- 服务端必须验证请求字段、复用全局代理设置、设置有限超时且不记录 API Key。
- 界面应显示明确的成功或失败结果，不持久化本次测试填写的数据。
- 完成后将 `VERSION` 从 1.9.6 提升一个补丁版本。
